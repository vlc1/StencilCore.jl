var documenterSearchIndex = {"docs":
[{"category":"section","location":"guide/#Guide","page":"Guide","text":"This guide builds the StencilCore types by hand. In normal use you would obtain a stencil from StencilCalculus (by differentiation) and hand it to StencilAssembly (to assemble a matrix); here we look under the hood.","title":"Guide"},{"category":"section","location":"guide/#Lattice-offsets","page":"Guide","text":"Offsets are type-level: a StaticShift is a normalized collection of StaticPair{D,O} (axis D, offset O). They support an algebra and print like lattice vectors, with ô the zero shift and ê₁ … ê₉ the unit shifts:\n\nusing StencilCore\n\n-2ê₁              # StaticShift{Tuple{StaticPair{1,-2}}}\n3ê₁ + ê₂          # StaticShift{Tuple{StaticPair{1,3}, StaticPair{2,1}}}\nê₁ - ê₁           # ô  (the zero shift)\n\nSame-axis pairs are summed, zero offsets dropped, and the result is sorted by axis — so the representation is canonical.","title":"Lattice offsets"},{"category":"section","location":"guide/#The-scalar-algebra","page":"Guide","text":"A scalar — a timestep, a Reynolds number, a literal 2.0 — has no spatial extent. StencilCore models it directly with AbstractScalar{T} and a small CAS that mirrors the term-side one. Six concrete types:\n\nSymbolic{S,T} — a named substitution leaf, like a Slot but with one materialized value instead of an array.\nConstant{T} — a literal leaf carrying its val::T. T is any concrete type — Number, SVector, anything. This is what numeric (and non-Number) literals canonicalise to at the operator boundary.\nScaling{T,V<:Number} — a numerical coefficient times the multiplicative identity of shape T: materialises to val * one(T). Compact carrier for \"a scalar multiple of I\", produced by the chain rule when differentiating vector-valued expressions.\nScalar{F,A,T} — an interior tree node fn(args…).\nNull{T} / Unity{T} — type-level structural 0 / 1. Unity requires one(T) defined (Number, square SMatrix); its outer ctor routes an SVector through to its square Jacobian space.\n\nThe operator overloads build Scalar trees, with numeric literals canonicalising to Constant:\n\n@symbolic τ Float64\nα = Constant(2.0)\n\nτ * α + α              # Scalar(+, (Scalar(*, (τ, α)), α))\ntypeof(τ * α + α)      # Scalar{typeof(+), …, Float64}\neltype(τ * α + α)      # Float64\n\nsimplify post-walks a scalar tree with the same rule-rewriter shape as the term side. Identities are purely structural — Null + x → x, x * Unity → x (with an eltype-preservation gate), x * Null → Null — never inspecting .val. Folding combines values; it does not match on them.\n\nsimplify(Null{Float64}() + τ)              # τ\nsimplify(τ * Unity{Float64}())             # τ\nsimplify(Constant(2.0) + Constant(3.0))    # Constant(5.0)\nsimplify(τ * Constant(1.0))                # stays a Scalar — numerical `1`\n                                           # is not a structural identity\n\ndifferentiate on a scalar tree returns an AbstractScalar whose eltype is the Jacobian of eltype(s) w.r.t. eltype(v). For Number variables the Jacobian is a Number; for SVector{N} variables it is the square SMatrix{N, N}. Mixed shape-classes are rejected at the top level.\n\n@symbolic τ Float64\ndifferentiate(sin(τ) * τ, τ)               # cos(τ)*τ + sin(τ)*1 simplified\ndifferentiate(Constant(2.0), τ)            # Null{Float64}() — no dependence\n\n# Vector-valued: ∂(2x)/∂x = 2I as a Scaling-carried SMatrix.\n@symbolic x SVector{2, Float64}\ndifferentiate(2x, x)                       # Scaling{SMatrix{2,2,Float64,4}}(2)\n\nmaterialize reduces a scalar tree to a single value, substituting Symbolic leaves from a NamedTuple:\n\nmaterialize(τ * Constant(3.0), (τ = 4.0,))  # 12.0\nmaterialize(Unity{SMatrix{2,2,Float64,4}}())  # the 2×2 identity matrix\n\nThese scalars become term coefficients once StencilCalculus wraps them in a Fill — see its docs for the bridge.","title":"The scalar algebra"},{"category":"section","location":"guide/#Linear-and-star-stencils","page":"Guide","text":"A LinearStencil has contiguous offsets along one mesh axis. Its coefficient is one SVector per cell, holding every per-offset coefficient on that column in ascending-offset order:\n\nusing StencilCore, StaticArrays\nn = 5\n# offsets 0,1 along axis 1; coefficients (-1, 1) at every column.\nfwd = LinearStencil{1}(SUnitRange(0, 1), fill(SVector(-1.0, 1.0), n))\n\nA StarStencil is the N-D star with symmetric reach −L … +L per axis, stored interlaced: a single SVector{2NL+1} per cell, in reverse-lex offset order, with the diagonal as the explicit middle slot — so a free diagonal term (Helmholtz, an unsteady term) has a home.\n\nn1, n2 = 5, 4\n# 2-D five-point Laplacian (L = 1 ⇒ M = 5):\n# (axis2,−1), (axis1,−1), diagonal, (axis1,+1), (axis2,+1).\nlap = StarStencil{1}(fill(SVector(-1.0, -1.0, 4.0, -1.0, -1.0), n1, n2))","title":"Linear and star stencils"},{"category":"section","location":"guide/#The-general-stencil-and-narrowing","page":"Guide","text":"Stencil is the lingua franca: an explicit reverse-lex list of StaticShift offsets plus a matching SVector coefficient. It is not assembled directly — as_linear / as_star narrow it to an assemblable type by matching the offset pattern and reusing the coefficient verbatim:\n\nst = Stencil(ColumnAccess, (-2ê₁, -ê₁, ô), fill(SVector(1.0, -4.0, 3.0), n))\nln = as_linear(st)        # LinearStencil{1, -2, 3, …}\nln.term === st.term       # true — verbatim copy\n\nA Stencil whose offsets form the canonical star pattern narrows with as_star instead; mismatched patterns raise an ArgumentError.","title":"The general stencil and narrowing"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"type","location":"api/#StencilCore.ArrayOrTermLike","page":"API reference","text":"ArrayOrTermLike{T} = Union{AbstractArray{T}, AbstractTerm{T}}\n\nA coefficient that is either a concrete array (assemblable) or a symbolic term (must be materialized first), both with element type T. Stencil types parameterise their coefficient field over this union.\n\n\n\n\n\n","title":"StencilCore.ArrayOrTermLike"},{"category":"constant","location":"api/#StencilCore.ô","page":"API reference","text":"ô, ê₁ … ê₉\n\nPredefined StaticShift constants: ô is the zero shift (identity), and êᵢ is the unit offset along axis i. Combine them with the +/-/*Int algebra to write lattice offsets, e.g. -2ê₁, 3ê₁ + ê₂.\n\n\n\n\n\n","title":"StencilCore.ô"},{"category":"type","location":"api/#StencilCore.AbstractScalar","page":"API reference","text":"AbstractScalar{T}\n\nSupertype for cell-level scalar expressions. Reaches a single value of type T at materialize time (no axes). Concrete subtypes: Symbolic, Constant, Scaling, Unity, Null, Scalar. Sibling of, not subtype of, AbstractTerm.\n\n\n\n\n\n","title":"StencilCore.AbstractScalar"},{"category":"type","location":"api/#StencilCore.AbstractStencil","page":"API reference","text":"AbstractStencil{S<:AccessStyle}\n\nAbstract supertype for every stencil. Subtypes (LinearStencil, StarStencil, Stencil, …) carry the access style S as their last type parameter and declare <: AbstractStencil{S}.\n\nProvides the AccessStyle trait accessor — subtypes inherit it without redefining.\n\n\n\n\n\n","title":"StencilCore.AbstractStencil"},{"category":"type","location":"api/#StencilCore.AbstractTerm","page":"API reference","text":"AbstractTerm{T}\n\nSupertype of every symbolic grid expression. An AbstractTerm{T} behaves like a dimension- and size-less array whose eltype is T: its grid rank N is unknown until it is materialized against concrete arrays, but its element type T (the value each cell will hold once materialized) is fixed at construction.\n\neltype(::AbstractTerm{T}) === T. Concrete subtypes are provided by the StencilCalculus package.\n\n\n\n\n\n","title":"StencilCore.AbstractTerm"},{"category":"type","location":"api/#StencilCore.AccessStyle","page":"API reference","text":"AccessStyle\n\nHoly trait reporting how a stencil's coefficient arrays are anchored:\n\nColumnAccess: term[c_idx...][k] is the value at column mesh position c_idx. Required for assembly into SparseMatrixCSC.\nRowAccess: term[r_idx...][k] is the value at row mesh position r_idx. Required for assembly into a row-major format (CSR; not yet implemented).\n\nThe trait is a type parameter on the stencil, not a runtime field. Assemblers dispatch on it; mismatching the access style and the target sparse format is a dispatch-time error (MethodError).\n\n\n\n\n\n","title":"StencilCore.AccessStyle"},{"category":"type","location":"api/#StencilCore.ColumnAccess","page":"API reference","text":"ColumnAccess <: AccessStyle\n\nCoefficients anchored at the column mesh position — required for compressed-sparse-column (SparseMatrixCSC) assembly. See AccessStyle.\n\n\n\n\n\n","title":"StencilCore.ColumnAccess"},{"category":"type","location":"api/#StencilCore.Constant","page":"API reference","text":"Constant{T}(val) / Constant(val)\n\nLiteral scalar leaf carrying a value val::T as-is. Materializes to val (no one(·) multiplication, unlike Scaling). T is any concrete type — Number, SArray, etc. — making Constant the right carrier for boundary literals such as x + 1, v + SVector(1) where no multiplicative identity of T is needed (or even defined).\n\n\n\n\n\n","title":"StencilCore.Constant"},{"category":"type","location":"api/#StencilCore.LinearStencil","page":"API reference","text":"LinearStencil{D, O, L, E<:SVector{L}, A<:ArrayOrTermLike{E}, S<:AccessStyle}\n    <: AbstractStencil{S}\n\nVariable-coefficient stencil with contiguous offsets, aligned with mesh dimension D. Offsets are diagonal indices: for a column j and a row i the diagonal number is k = j − i. For column c at mesh position p_c and offset δ, the matrix entry lands on row p_c − δ with coefficient term[p_c][δ − O + 1] (column-anchored under S = ColumnAccess); each element term[p_c] is the SVector{L} of all L per-offset coefficients on that column in ascending offset order (term[p_c][1] ↦ offset O).\n\nType parameters:\n\nD: mesh dim along which the stencil acts (D ≥ 1; for a concrete coefficient also D ≤ ndims(term)).\nO = δ_min, L = δ_max − δ_min + 1 — static via SUnitRange.\nE<:SVector{L}: coefficient element type (the per-column SVector); the scalar is eltype(E).\nA<:ArrayOrTermLike{E}: the coefficient container — a concrete AbstractArray{E} (assemblable) or a symbolic AbstractTerm{E}. N (grid rank) is not a stencil parameter: it is ndims(A) for a concrete coefficient, and unknown (resolved at materialize) for a symbolic one.\nS<:AccessStyle: coefficient anchoring (ColumnAccess for CSC).\n\nThe inner constructors bind E = SVector{L, T}; the default outer constructor defaults S to ColumnAccess. A catch-all reports ArgumentErrors for non-SUnitRange offsets and ill-typed coefficients.\n\n\n\n\n\n","title":"StencilCore.LinearStencil"},{"category":"type","location":"api/#StencilCore.Null","page":"API reference","text":"Null{T}()\n\nType-level additive identity / structural zero for AbstractScalar: the scalar-side analogue of AbstractTerm Zero. Materializes to zero(T); lets the scalar simplify and differentiate rules collapse by dispatch.\n\n\n\n\n\n","title":"StencilCore.Null"},{"category":"type","location":"api/#StencilCore.RowAccess","page":"API reference","text":"RowAccess <: AccessStyle\n\nCoefficients anchored at the row mesh position — reserved for a future compressed-sparse-row backend. See AccessStyle.\n\n\n\n\n\n","title":"StencilCore.RowAccess"},{"category":"type","location":"api/#StencilCore.SPair","page":"API reference","text":"SPair\n\nAlias for StaticPair.\n\n\n\n\n\n","title":"StencilCore.SPair"},{"category":"type","location":"api/#StencilCore.SShift","page":"API reference","text":"SShift\n\nAlias for StaticShift.\n\n\n\n\n\n","title":"StencilCore.SShift"},{"category":"type","location":"api/#StencilCore.Scalar","page":"API reference","text":"Scalar(fn, args::Tuple{Vararg{AbstractScalar}})\n\nInterior node of a scalar-tree: applies fn to scalar args component-wise. The element type T = Base.promote_op(fn, eltype.(args)...) is computed at construction; a Union{} result throws (the node is unconstructable). Term-side analogue: AbstractTerm Term.\n\n\n\n\n\n","title":"StencilCore.Scalar"},{"category":"type","location":"api/#StencilCore.Scaling","page":"API reference","text":"Scaling{T, V <: Number}(val)\n\nLiteral scalar leaf representing the value val * one(T). Two type parameters:\n\nT           — the materialized container type (the shape one(·) is taken of). Listed first so the one-curly inner ctor Scaling{T}(val) binds the curly argument to the shape tag. T = T_raw whenever V <: eltype(T_raw); otherwise T widens via numeric promotion (e.g. Scaling{Float32}(1.0) yields T = Float64).\nV <: Number — type of the stored val.\n\nΛ is an alias.\n\nThe isotropic boundary form Scaling(val::Number) is not provided — numeric literals at the operator boundary canonicalise to Constant instead. Every Scaling carries an explicit shape tag.\n\nConstructors:\n\nScaling{T}(val)                           — explicit shape tag T.\nScaling(T::Type)                          — unit Scaling in the linear- map space of T: a Number stays T; an SVector{N, F} maps to SMatrix{N, N, F} (its Jacobian space). val = one(eltype(·)).\nScaling(T::Type, val)                     — same T → linear-map space mapping, with explicit val stored as-is (V = typeof(val)).\nScaling(::Type{<:AbstractScalar{S}})      — convenience: delegates to Scaling(S).\nScaling(::Type{<:AbstractScalar{S}}, val) — delegates to Scaling(S, val).\n\n\n\n\n\n","title":"StencilCore.Scaling"},{"category":"type","location":"api/#StencilCore.StarStencil","page":"API reference","text":"StarStencil{L, N, M, E<:SVector{M}, A<:ArrayOrTermLike{E}, S<:AccessStyle}\n    <: AbstractStencil{S}\n\nN-D variable-coefficient star-shaped stencil with symmetric reach −L … +L along every mesh dimension, stored interlaced: a single coefficient term::A whose element at each cell is the SVector{M} of the whole star, M = 2NL + 1. The entries are in reverse-lexicographic offset order (axis N most significant — the CartesianIndex order) with the diagonal as the explicit middle slot (M+1)/2. Unlike a per-axis decomposition, the diagonal is a single free coefficient (Helmholtz k², parabolic ∂ₜ), not a sum of per-axis centers.\n\nFor L = 2, N = 3 (M = 13) the slot ↦ offset map is:\n\n1:(d3,-2) 2:(d3,-1) 3:(d2,-2) 4:(d2,-1) 5:(d1,-2) 6:(d1,-1) 7:(diag)\n8:(d1,+1) 9:(d1,+2) 10:(d2,+1) 11:(d2,+2) 12:(d3,+1) 13:(d3,+2)\n\nType parameters:\n\nL ≥ 1 per-axis reach; M = 2NL + 1 whole-star offset count.\nN grid rank, kept explicit; checked to equal (M-1)/(2L) (and the coefficient array's ndims, when concrete).\nE<:SVector{M} coefficient element type; scalar eltype(E).\nA<:ArrayOrTermLike{E}: the (single) coefficient container — concrete array (assemblable) or symbolic term.\nS<:AccessStyle.\n\n\n\n\n\n","title":"StencilCore.StarStencil"},{"category":"type","location":"api/#StencilCore.StaticPair","page":"API reference","text":"StaticPair{D, O}\n\nA single offset of magnitude O along mesh dimension D (both compile-time Ints). Singleton. Alias: SPair.\n\n\n\n\n\n","title":"StencilCore.StaticPair"},{"category":"type","location":"api/#StencilCore.StaticShift","page":"API reference","text":"StaticShift{P<:Tuple{Vararg{StaticPair}}}\n\nA lattice offset: a normalized collection of StaticPairs. Invariants (enforced by the constructor): pairs are sorted ascending by dimension, no two pairs share a dimension (same-dimension pairs are summed), and no pair has zero offset (dropped). The empty shift StaticShift{Tuple{}} is the identity.\n\nConstruct from pairs, or via the basis symbols ê₁ … ê₉ and the +/-/*Int algebra:\n\n3ê₁ + ê₂            # StaticShift{Tuple{StaticPair{1,3}, StaticPair{2,1}}}\n\nAlias: SShift.\n\n\n\n\n\n","title":"StencilCore.StaticShift"},{"category":"type","location":"api/#StencilCore.Stencil","page":"API reference","text":"Stencil{M, C<:NTuple{M,StaticShift}, A<:NTuple{M,ArrayOrTermLike}, S<:AccessStyle}\n    <: AbstractStencil{S}\n\nGeneral offset-list stencil in structure-of-arrays form: M offsets shifts (reverse-lexicographically ordered) and a parallel M-tuple terms of per-offset coefficients (terms[k] is the coefficient at offset shifts[k]). Each coefficient is an ArrayOrTermLike — a concrete array or a symbolic term — with a scalar element type.\n\nThe form produced by symbolic differentiation; not assembled directly — narrowed to a LinearStencil / StarStencil via as_linear / as_star, which is where the layout switches to array-of-structs (one SVector{M}-valued coefficient) by _interlaceing terms.\n\n\n\n\n\n","title":"StencilCore.Stencil"},{"category":"type","location":"api/#StencilCore.Symbolic","page":"API reference","text":"Symbolic{S, T}()\n\nNamed, runtime-substituted scalar parameter S (a Symbol) of concrete type T (default Float64). Materializes to the value supplied at the keyword S of the pairs NamedTuple — like Slot but without per-cell indexing.\n\n\n\n\n\n","title":"StencilCore.Symbolic"},{"category":"type","location":"api/#StencilCore.Unity","page":"API reference","text":"Unity{T}()\n\nType-level multiplicative identity / structural one for AbstractScalar. Materializes to one(T); requires T to be a square scalar — a type with one(T) defined (Number, square SMatrix{N,N,F}, …). Construction rejects T lacking one(T) (e.g. SVector, non-square SMatrix).\n\nLets the scalar simplify rules collapse multiplicative identities by dispatch — structurally, with no .val inspection — mirroring how Null collapses additive identities.\n\n\n\n\n\n","title":"StencilCore.Unity"},{"category":"type","location":"api/#StencilCore.Λ","page":"API reference","text":"Λ\n\nAlias for Scaling.\n\n\n\n\n\n","title":"StencilCore.Λ"},{"category":"function","location":"api/#StencilCore._interlace","page":"API reference","text":"_interlace(terms::NTuple{M, ArrayOrTermLike}) -> ArrayOrTermLike{<:SVector{M}}\n\nCombine M per-offset (structure-of-arrays) coefficients into the single array-of-structs SVector{M}-valued coefficient that LinearStencil / StarStencil store — the representation switch performed by narrowing.\n\nThe concrete-array method (stack element-wise into an array of SVector{M}) lives here; the symbolic-term method (Term(SVector, terms)) is added by the StencilCalculus package.\n\n\n\n\n\n","title":"StencilCore._interlace"},{"category":"method","location":"api/#StencilCore._scalar_body_expr-Union{Tuple{Symbolic{S}}, Tuple{S}} where S","page":"API reference","text":"_scalar_body_expr(s::AbstractScalar) -> Expr or literal\n\nLower a scalar tree to an Expr that evaluates it in the StencilCalculus codegen args::NamedTuple context (no per-cell indices — scalars are position-independent). Used by _body_expr(::Fill{<:AbstractScalar}, …) on the term side. Returns an Expr suitable for embedding in a larger AST.\n\n\n\n\n\n","title":"StencilCore._scalar_body_expr"},{"category":"method","location":"api/#StencilCore.as_linear-Union{Tuple{Stencil{M, C, A, S}}, Tuple{S}, Tuple{A}, Tuple{C}, Tuple{M}} where {M, C, A, S}","page":"API reference","text":"as_linear(st::Stencil{…,S}) -> LinearStencil{D, …, S}\n\nNarrow a Stencil whose offsets are single-axis (same axis D) and contiguous to the equivalent LinearStencil{D}, interlacing terms into the single SVector{D}-valued coefficient. Throws if the offsets are multi-axis, span several axes, or are not contiguous-ascending.\n\n\n\n\n\n","title":"StencilCore.as_linear"},{"category":"method","location":"api/#StencilCore.as_star-Union{Tuple{Stencil{M, C, A, S}}, Tuple{S}, Tuple{A}, Tuple{C}, Tuple{M}} where {M, C, A, S}","page":"API reference","text":"as_star(st::Stencil{…,S}) -> StarStencil{L, …, S}\n\nNarrow a Stencil whose offsets form the canonical reverse-lex star pattern (every axis 1..N with symmetric reach −L..L, plus the diagonal) to the equivalent StarStencil{L}, interlacing terms into the single SVector{M}- valued coefficient. Throws otherwise.\n\n\n\n\n\n","title":"StencilCore.as_star"},{"category":"function","location":"api/#StencilCore.derivative","page":"API reference","text":"derivative(f, ::Val{i}, args...) -> AbstractScalar\n\nThe symbolic partial derivative ∂f/∂(argᵢ) of a primitive f applied to scalar args (ChainRules frule style). The 0/1 cases use the structural Null / Unity carriers via _unity (which routes through _unity_space so a Number leaf stays Number and an SVector leaf lands in the square SMatrix Jacobian space).\n\n\n\n\n\n","title":"StencilCore.derivative"},{"category":"method","location":"api/#StencilCore.differentiate-Tuple{AbstractScalar, Symbolic}","page":"API reference","text":"differentiate(s::AbstractScalar, v::Symbolic{S}) -> AbstractScalar\n\nDifferentiate s with respect to the named scalar parameter v (matched on the symbol only). The result is an AbstractScalar whose eltype is the Jacobian element type J = _jacobian_type(eltype(s), eltype(v)): promote_type(Tout, Tin) for Number/Number, SMatrix{N, N, promote_type(F1, F2)} for matching-N SVector{N, F1} / SVector{N, F2}. Mixed shape-classes throw. Returns Null{J}() when s does not depend on v; otherwise the chain-rule expression, simplified. Scalar-side analogue of StencilCalculus.differentiate.\n\n\n\n\n\n","title":"StencilCore.differentiate"},{"category":"method","location":"api/#StencilCore.dim-Union{Tuple{StaticPair{D, O}}, Tuple{O}, Tuple{D}} where {D, O}","page":"API reference","text":"dim(p), offset(p)\n\nDimension D and offset O of a StaticPair (works on the instance or the type).\n\n\n\n\n\n","title":"StencilCore.dim"},{"category":"method","location":"api/#StencilCore.materialize-Union{Tuple{S}, Tuple{Symbolic{S}, NamedTuple}} where S","page":"API reference","text":"materialize(s::AbstractScalar, pairs::NamedTuple = (;))\n\nSubstitute the Symbolic leaves named in pairs into s and reduce to a single value of type eltype(s). The scalar-side analogue of StencilCalculus.materialize.\n\n\n\n\n\n","title":"StencilCore.materialize"},{"category":"function","location":"api/#StencilCore.simplify","page":"API reference","text":"simplify(s::AbstractScalar, rules = SCALAR_DEFAULT_RULES; maxsteps = 64)\n\nRewrite a scalar tree to a normal form by post-walking and applying rules to a fixed point. The default rules are purely structural: identities and annihilators dispatch on Null (additive zero) and Unity (multiplicative one, with an eltype-preservation gate), never on .val. Folding combines numerical coefficients across Constant / Scaling / Unity / Null args (Path 1) or direct values across all-Constant args (Path 2). The scalar-side analogue of StencilCalculus.simplify.\n\n\n\n\n\n","title":"StencilCore.simplify"},{"category":"macro","location":"api/#StencilCore.@symbolic","page":"API reference","text":"@symbolic name [T = Float64]\n\nBind name to Symbolic{:name, T}(). @symbolic τ ≡ τ = Symbolic{:τ, Float64}(); @symbolic τ Float32 ≡ τ = Symbolic{:τ, Float32}(). Term-side analogue: @slot.\n\n\n\n\n\n","title":"StencilCore.@symbolic"},{"category":"section","location":"#StencilCore.jl","page":"Home","text":"StencilCore is the shared type vocabulary at the root of a small stack for building and assembling sparse operators on structured (Cartesian) meshes. It owns the types and a small scalar CAS; it depends only on StaticArrays.jl and AbstractTrees.jl.\n\nStencilCore              stencil types: AccessStyle, AbstractStencil, AbstractTerm{T},\n  │                                     ArrayOrTermLike, StaticShift, LinearStencil,\n  │                                     StarStencil, Stencil, as_linear / as_star\n  │                      scalar CAS:    AbstractScalar{T}, Symbolic, Constant,\n  │                                     Scaling, Scalar, Null, Unity,\n  │                                     simplify, materialize, differentiate\n  ├── StencilAssembly      CSC assembly  (build / assemble / update!)\n  └── StencilCalculus      term-level CAS (simplify, differentiate, materialize) + bridge\n\nYou probably want one of the leaves: StencilCalculus to build a stencil by differentiating a symbolic grid expression, or StencilAssembly to assemble a stencil into a sparse matrix. This page explains the vocabulary both of them speak.","title":"StencilCore.jl"},{"category":"section","location":"#Why-a-stencil-needs-a-vocabulary","page":"Home","text":"On a structured mesh, a discrete field is stored as an ordinary N-D array and the connectivity is implicit: the neighbours of node (i, j) are (i±1, j) and (i, j±1), reached by offsetting the index. A great many numerical operations — finite differences, Laplacians — are stencils: the same local formula relating a cell to its neighbours, applied at every point.\n\nA stencil, then, is just a collection of (offset, coefficient) pairs. The two facts about that collection live at very different levels:\n\nthe offsets are structure — a tiny, compile-time-known set that fixes the sparsity pattern and the index arithmetic;\nthe coefficients are data — arbitrary numbers (often position-dependent, e.g. a density-weighted gradient (ϕ[i] − ϕ[i−1]) / ρ[i]).\n\nnote: Motto\nType parameters are structure; values are data.\n\nStencilCore encodes that split directly: offsets are type-level (StaticShift), coefficients are ordinary (or lazy) arrays, and a stencil's element type is an SVector of all the per-offset coefficients on a single column.","title":"Why a stencil needs a vocabulary"},{"category":"section","location":"#Two-parallel-algebras","page":"Home","text":"The CAS half of StencilCore is built from two sibling type hierarchies that mirror each other:\n\nrole AbstractScalar (Core) AbstractTerm (StencilCalculus)\nnamed substitution leaf Symbolic{S, T} Slot{S, T}\nliteral carrier Constant{T} with val::T — (literals enter terms via Fill(Constant(…)))\nscaled identity Scaling{T, V<:Number} (val * one(T)) —\ninterior tree node Scalar{F, A<:Tuple{Vararg{AbstractScalar}}} Term{F, A<:Tuple{Vararg{AbstractTerm}}}\nadditive identity Null{T} (structural) Zero{T}\nmultiplicative identity Unity{T} (structural; requires one(T)) One{T}\nbroadcast bridge — Fill{T} (lives in StencilCalculus, wraps an AbstractScalar or a literal)\n\nAn AbstractTerm{T} is array-like — a dimension-/size-less collection that materializes to a per-cell array. An AbstractScalar{T} is not array-like — it materializes to a single value. The two are bridged in StencilCalculus by the Fill term, which broadcasts a scalar across the grid. Scalars never appear inside a Term directly — they enter via Fill, so Term.args stays Tuple{Vararg{AbstractTerm}}.","title":"Two parallel algebras"},{"category":"section","location":"#The-pieces","page":"Home","text":"AccessStyle — a trait (ColumnAccess / RowAccess) recording whether a stencil's coefficients are anchored at the column or the row index; ColumnAccess is what a compressed-sparse-column matrix wants.\nAbstractTerm{T} — \"a dimension-/size-less array-like object whose element type is T\", and ArrayOrTermLike{T} = Union{AbstractArray{T}, AbstractTerm{T}}, the coefficient type of every stencil. A stencil is assemblable with a concrete-array coefficient and symbolic with a term coefficient.\nAbstractScalar{T} — the cell-level scalar algebra. Concrete leaves (Symbolic, Constant, Scaling, Null, Unity) plus the interior Scalar node have their own operator overloads, a structural simplify rewriter (identities by dispatch on Null/Unity; folding combines values but does not match on them), a materialize reduction, and a Jacobian-aware differentiate chain rule — all parallel to the term side, all living entirely in scalar-land.\nStaticPair{D,O} / StaticShift — type-level lattice offsets with a +/-/* algebra, the zero shift ô, and basis shifts ê₁ … ê₉. They read like lattice vectors:\nusing StencilCore\n3ê₁ + ê₂        # StaticShift{Tuple{StaticPair{1,3}, StaticPair{2,1}}}\nLinearStencil (contiguous offsets along one axis), the interlaced StarStencil (the whole star per cell, with the diagonal as an explicit slot), and the general Stencil (an arbitrary reverse-lex offset list — the form differentiation produces).\nas_linear / as_star — narrow a general Stencil to an assemblable LinearStencil / StarStencil by a shift-pattern match and a verbatim coefficient copy.\n\nusing StencilCore, StaticArrays\n# A 1-D forward-difference stencil: offsets 0 and 1, one SVector of\n# coefficients per column.\nst = LinearStencil{1}(SUnitRange(0, 1), fill(SVector(-1.0, 1.0), 5))\n\n# The scalar CAS, on its own:\n@symbolic τ Float64\nα = Constant(2)\ndifferentiate(τ * α + α, τ)        # === Constant{Float64}(2.0)\n                                   # eltype promotes via Scalar(*); broadcasts to `2.0` per cell once wrapped in Fill\n\nSee the Guide for worked examples and the API reference for the full surface.","title":"The pieces"}]
}
